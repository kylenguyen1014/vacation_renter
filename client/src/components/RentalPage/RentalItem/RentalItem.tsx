import React, { ReactElement } from 'react'
import { Rental } from '../../../shared/interfaces/Rental'

interface Props {
    rental : Rental
}

function RentalItem({rental}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default RentalItem
